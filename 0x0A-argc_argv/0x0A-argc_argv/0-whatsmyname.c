#include <stdio.h>
/**
 * main - entry point
 * @argc: counts the arguments that supplied to the program
 * @argv: an array of pointers to the strings which are those arguments
 * Return: 0
 */
int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);
	return (0);
}
