#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char sayHello[] = "\"Programming is like building a multilingual puzzle";

	puts(sayHello);

	return (0);
}
